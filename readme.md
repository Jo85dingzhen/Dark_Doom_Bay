# Dark Doom Bay (15KB mobile/web)

A single-file canvas FPS demo that runs directly in the browser (desktop + iOS/Android). No builds, no deps.

## Run

### Option 1: Double‑click
- Open `index.html` in any modern browser.
- For iOS/Safari: add to home screen or open directly; orientation changes are handled.

### Option 2: Local server (recommended for pointer-lock on desktop)
```sh
cd "Dark Doom Bay"
python -m http.server 8000
# then visit http://localhost:8000/index.html
